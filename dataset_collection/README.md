All the file naming a bit odd, but I lazy to change all

training: Here it means the dataset collection (training or testing), can think the dataset to feed in ML
testing: Here it means the evaluation dataset using ML/DL prediction, so it will use back some configurations from pre-collected dataset (like location prosumer, etc.)